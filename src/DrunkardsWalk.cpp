#include "../header/DrunkardsWalk.hpp"

DrunkardsWalk:: DrunkardsWalk(GridPtr tiles, Size maxSize, int walkLen, float minCover): tiles(tiles), maxSize(maxSize), walkLen(walkLen), minCover(minCover)
{
    // leave empty, already initialized to walls in main.cpp
}

