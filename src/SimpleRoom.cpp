#include "../header/SimpleRoom.hpp"

// Constructor
SimpleRoom::SimpleRoom(int** tiles, int numRooms, Size minSize, Size maxSize)
    : tiles(tiles), numRooms(numRooms), minSize(minSize), maxSize(maxSize)
{
    // leave empty 
}

